# SageMaker test dependencies
# Note: Install requirements-base.txt first in CI/CD
sagemaker==2.235.2  # Pinned: >=2.236.0 requires omegaconf which conflicts with antlr4-python3-runtime in braket-default-simulator
