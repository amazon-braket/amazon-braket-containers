# SageMaker test dependencies
# Note: Install requirements-base.txt first in CI/CD
# Note: omegaconf>=2.4 is required to avoid antlr4 conflict between
#       sagemaker-core and amazon-braket-default-simulator.
#       Install with --pre until omegaconf 2.4 is released.
--pre
omegaconf==2.4.0.dev11
sagemaker==2.257.3
